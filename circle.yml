machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - scripts/ci/circle-prepare.sh
  cache_directories:
    - ~/dependencies/otp_src_18.2.4
    - ~/.mix
    - _build
    - deps

test:
  override:
    - scripts/ci/circle-tests.sh
